package ast;

import recursiveDescent.Token;
import visitor.Visitor;

public class UMinus extends Unary {

	public UMinus(Expr c, Token t) {
		super(c, t);
	}	
	public <T> T accept(Visitor<T> Visitor) {
		return Visitor.visit(this);
	}
}
