package ast;

import recursiveDescent.Token;
import recursiveDescent.Visitor;

public class Mul extends Binary {

	public Mul(Expr l, Token t, Expr r) {
		super(l, t, r);
	}
	public <T> T accept(Visitor<T> Visitor) {
		return Visitor.visit(this);
	}
}
